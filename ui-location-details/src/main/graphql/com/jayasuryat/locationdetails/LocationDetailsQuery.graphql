query LocationDetailsQuery($id: ID!) {
    location(id: $id) {
        id
        name
        type
        dimension
        residents {
            id
            name
            image
        }
    }
}
