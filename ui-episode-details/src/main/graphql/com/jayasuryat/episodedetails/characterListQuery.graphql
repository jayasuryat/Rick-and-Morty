query EpisodeDetail($id : ID!) {
    episode(id: $id) {
        id
        name
        air_date
        episode
        characters {
            id
            name
            image
        }
    }
}