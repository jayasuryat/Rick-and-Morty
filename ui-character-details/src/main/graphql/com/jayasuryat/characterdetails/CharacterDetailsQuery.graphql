query CharacterDetailsQuery($id: ID!) {
    character(id: $id) {
        id
        name
        status
        species
        type
        gender
        location {
            id
            name
            type
            dimension
        }
        image
        origin {
            id
            name
            type
            dimension
        }
    }
}
